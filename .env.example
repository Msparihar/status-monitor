# Required: Your OpenAI API key for AI-powered incident summaries (optional).
OPENAI_API_KEY=sk-...

# Public URL where this server is reachable (for webhook registration).
WEBHOOK_BASE_URL=https://your-domain.ngrok-free.dev

# Secret token embedded in the webhook URL path for security.
# If not set, a random UUID is generated on each startup.
WEBHOOK_SECRET=

# How often (seconds) the background poller checks all status pages.
POLL_INTERVAL_SECONDS=90

# Logging format: "pretty" (colorized console) or "json" (structured).
LOG_FORMAT=pretty
